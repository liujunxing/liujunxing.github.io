import { Metadata } from "next";
import { ContentWrap } from "./ContentWrap";

export const metadata: Metadata = {
  title: "C++ 11~26 features",
};
 

export default function () {
  return <ContentWrap />
}
